standardize_equation: |
  You are a control systems expert specializing in dynamic system modeling. Your task is to complete the provided Python template to generate
  solve_ivp-compatible dynamics code.

  **CRITICAL REQUIREMENTS:**
  1. Fill ONLY the designated sections (Steps 1, 2, 3, 4)
  2. Use only NumPy-compatible operations and functions
  3. Define ALL constants with numeric values in Step 1
  4. Define state vector X and input vector U in Step 2 with proper variable names
  5. Write the actual dynamics equations in Step 3
  6. The function must follow exact format allways: dxdt = system_dynamics(t, x, u)
  7. Name of function should allways be system_dynamics
  8. The returned `dxdt` object MUST be a flat 1D array/list (e.g., shape `(N,)` and NOT a 2D column vector like `(N,1)` or shape `[[value1], [value2]]`).

  **SYSTEM DESCRIPTION TO MODEL:**
  {equation}


  **TEMPLATE TO COMPLETE:**
  {schema}

  **OUTPUT REQUIREMENTS:**
  - Return ONLY the completed python-compatible code
  - No explanations, comments, or additional text outside the code
  - Preserve the exact function structure but delete template comments
  - Function must be compatible with Python's scipy.integrate.solve_ivp solver
  - Always take U, X and t as input of function and NEVER assume constant values for each of them.
  - DON'T Forget including import statements at top of file
  - Always Import numpuy with "from numpy import *"
  - Ensure the final return statement yields a strictly 1D array. Use `.flatten()` or `array([...]).reshape(-1)` if there is any ambiguity in matrix multiplications.


schema: |
  from numpy import *
  
  def system_dynamics(t, x, u):
      # =========================================================================
      # UNIVERSAL PLANT MODEL
      # Target: Python solve_ivp / State-Space
      #
      # Structure:
      #   x = [state1, state2, ..., stateN]
      #   u = [input1, input2, ..., inputM]
      # =========================================================================
  
      ## 1. PARAMETERS (Hard-coded constants)
      # [LLM: Define all coefficients, mass, inertia, or gains here]
      # Example: mass = 10; gravity = 9.81;
      # [PARAM_START]
  
      # [PARAM_END]
  
      ## 2. UNPACK STATES & INPUTS
      # [LLM: Mapping for readability]
      # [UNPACK_START]
      # Example: pos = x[0]; vel = x[1];
      # Example: force = u[0];
  
      # [UNPACK_END]
  
      ## 3. DYNAMICS / EQUATIONS OF MOTION
      # [LLM: Implement the differential equations]
      # If 2nd order: d_pos = vel; d_vel = (force - b*vel - k*pos)/m;
      # [EOM_START]
  
      # [EOM_END]
  
      ## 4. DERIVATIVE VECTOR CONSTRUCTION
      # [LLM: Must be a FLAT 1D vector/array matching the state order, e.g., array([d_pos, d_vel])]
      # [LLM: Must be a vector/array matching the state order]
      # [DERIVATIVE_START]
      dxdt = np.array([])
      # [DERIVATIVE_END]
  
      return dxdt
