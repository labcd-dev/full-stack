# corrupted.py
# This file contains multiple syntax errors, indentation issues, name errors,
# type errors, attribute errors, index/key errors, import errors, recursion,
# generator exhaustion, and other common Python mistakes.

# 1. Syntax: missing colon
if True
    print("missing colon")

# 2. Syntax: unmatched parenthesis
print("unclosed string

# 3. Syntax: missing closing bracket
my_list = [1, 2, 3

# 4. Syntax: invalid escape sequence
path = "C:\Users\name"

# 5. IndentationError: mixed tabs and spaces (tab before print)
def mixed_indent():
	print("tabbed")

# 6. IndentationError: missing indent inside block
def bad_indent():
print("no indent")

# 7. NameError: undefined variable
print(undefined_var)

# 8. NameError: undefined function
undefined_function()

# 9. TypeError: str + int
result = "answer: " + 42

# 10. TypeError: calling non-callable
x = 5
x()

# 11. TypeError: unsupported operand type for list + int
[1,2] + 3

# 12. AttributeError: list.append on string
s = "hello"
s.append("world")

# 13. AttributeError: NoneType.strip
nothing = None
nothing.strip()

# 14. IndexError: list index out of range
items = [1,2,3]
print(items[5])

# 15. KeyError: missing dict key
d = {"a":1}
print(d["b"])

# 16. ZeroDivisionError
value = 10 / 0

# 17. ImportError: missing module
from some_missing_module import useful_func
useful_func()

# 18. RecursionError (infinite recursion)
def recurse():
    recurse()
recurse()

# 19. StopIteration: exhausted generator
g = (x for x in range(2))
next(g); next(g); next(g)

# 20. ValueError: int() conversion
num = int("not a number")

# 21. FileNotFoundError
with open("nonexistent.txt") as f:
    content = f.read()

# 22. PermissionError (if run as non‑root, but we include pattern)
with open("/root/secret.txt", "w") as f:
    f.write("data")

# 23. UnicodeEncodeError
print("café".encode("ascii"))

# 24. SyntaxError: break outside loop
if True:
    break

# 25. SyntaxError: continue outside loop
def func():
    continue

# 26. RuntimeError: dict changed size during iteration
d2 = {1:2, 3:4}
for k in d2:
    d2[5] = 6

# 27. Asyncio: missing event loop
import asyncio
async def main():
    await asyncio.sleep(1)
main()

# 28. UnboundLocalError: variable referenced before assignment
def unbound():
    print(local_var)
    local_var = 5
unbound()

# 29. AssertionError
assert 1 == 2, "math is broken"

# 30. KeyboardInterrupt (pattern, not directly executable)
while True:
    pass

# 31. MemoryError (pattern)
huge = [0] * (10**10)

# 32. OverflowError (huge exponent)
big = 10 ** 1000000

# 33. DeprecationWarning (not an error, but tool may warn)
import warnings
warnings.warn("old function", DeprecationWarning)

# 34. SyntaxError: assignment to literal
123 = 456

# 35. SyntaxError: missing self in class method
class MyClass:
    def method():
        print("no self")

# 36. TypeError: missing required argument
def greet(name):
    print(f"Hello {name}")
greet()

# 37. Custom unhandled exception
class MyError(Exception):
    pass
raise MyError("oops")